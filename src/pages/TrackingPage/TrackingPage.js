import React from 'react';
import { Activity } from 'lucide-react';
import PlaceholderPage from '../PlaceholderPage';

const TrackingPage = () => {
	return (
		<PlaceholderPage
			title='Tracking'
			icon={Activity}
			description='Monitor your symptoms, medications, and daily activities.'
		/>
	);
};

export default TrackingPage;
